<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the real UI; the platform theme only needs to stay out of the way. -->
    <style name="Theme.RemoteControl" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
